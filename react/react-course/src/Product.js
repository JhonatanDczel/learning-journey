function Product() {
  return <div>
    <h1>Product component</h1>
  </div>
}

export function Navbar(){
  return <nav>Navigation</nav>
}

// Exporta todo el modelo, todo el archivo, y se tiene que nombrar manualmente a estas funciones
export default Product